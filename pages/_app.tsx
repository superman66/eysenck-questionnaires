import "../styles/globals.css";
import type { AppProps } from "next/app";

function EysenckQuestionnaire({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}

export default EysenckQuestionnaire;
